
# Wrangler Configuration for PolyVis Reactor

name = "polyvis-reactor"
compatibility_date = "2024-04-01"
pages_build_output_dir = "./dist/public"

# We don't strictly need a 'functions' binding for Pages unless complex,
# Pages automatically treats the /functions directory as routing.
