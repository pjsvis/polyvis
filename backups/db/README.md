# Database Backups

This directory contains snapshots of `public/resonance.db`.

## Backups
- `resonance.[timestamp].db`: Created before the **Unified Semantic Layer** teardown. 
  - **Reason:** To preserve the pre-refactor state (Probabilistic Graph) for comparison if needed.
  - **Pipeline:** Pre-AST, Pre-StrictEdge, Pre-LouvainGate.
